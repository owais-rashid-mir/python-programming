# Using built-in or standard library modules

import sys
print("\nPython path = \n", sys.path)

# For every standalone program written by the user, the name of the module is __main__
print("Name of the module is: ", __name__)