age = int(input("Enter your age:"))

# You can also use the parenthesis like:    if(age >= 18):
# But PyCharm IDE shows a yellow warning line if you use brackets, so avoid using them.
if age >= 18:
    print("You're eligible to vote.")